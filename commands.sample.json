{
  "rules": [
	{
		"event": "push",
		"reponame": "reponame",
		"ref": "refs/heads/master",
		"precommands": [
			"cd /var/log && ls -la",
			"cd /mnt && ls",
            "node node_modules/.bin/shipit staging deploy"
		],
		"deploy": false
    }
  ]

}
